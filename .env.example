# Root-level app variables (fill when app starts using these)

# API server
API_HOST=0.0.0.0
API_PORT=8000

# Game server
GAMESERVER_PORT=9001

# Application-level connection strings
# Keep these in sync with infra/.env for local Docker usage.
DATABASE_URL=postgresql://secure_chess_user:change_me_postgres@localhost:5432/secure_chess
REDIS_URL=redis://:change_me_redis@localhost:6379
